<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout android:id="@+id/activity_login"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="ir.evoteam.ghmap.LoginActivity"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <EditText android:layout_width="wrap_content"
        android:layout_height="wrap_content"

        android:ems="10"
        android:hint="password"
        android:id="@+id/UserNameEditText"
        android:layout_centerVertical="true"
        android:layout_alignStart="@+id/PassWordEditText"/>
    <EditText android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textPersonName"
        android:ems="10"
        android:hint="user name"
        android:id="@+id/PassWordEditText"
        android:layout_marginBottom="12dp"
        android:layout_above="@+id/UserNameEditText"
        android:layout_centerHorizontal="true"/>
    <Button android:text="login"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/UserNameEditText"
        android:layout_alignParentStart="true"
        android:layout_marginStart="14dp"
        android:layout_marginTop="18dp"
        android:id="@+id/LogInButtonInLoginPage"/>
    <Button android:text="exit"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/LogInButtonInLoginPage"
        android:layout_alignParentEnd="true"
        android:id="@+id/ExitButtonInLoginPage"/>
</RelativeLayout>
