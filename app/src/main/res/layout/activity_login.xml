<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:app="http://schemas.android.com/apk/res-auto" android:id="@+id/activity_login"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingBottom="@dimen/activity_vertical_margin"
                android:paddingLeft="@dimen/activity_horizontal_margin"
                android:paddingRight="@dimen/activity_horizontal_margin"
                android:paddingTop="@dimen/activity_vertical_margin"
                tools:context="ir.evoteam.evomap.LoginActivity"
                xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:scrollbars="vertical"


    >

    <EditText android:layout_width="match_parent"
              android:layout_height="wrap_content"
              android:hint="@string/user_name"
              android:id="@+id/UserNameEditText"
              android:gravity="left"
              android:textColor="@color/yellow"
              android:textColorHint="@color/yellow"
              android:layout_alignBottom="@+id/PassWordEditText"
              android:layout_alignParentStart="true" android:layout_marginBottom="61dp"/>
    <Button android:text="login"
            android:background="@color/yellow"

            android:layout_width="match_parent"
            android:layout_height="wrap_content"

            android:id="@+id/LogInButtonInLoginPage"
            android:layout_above="@+id/ExitButtonInLoginPage"
            android:layout_alignParentStart="true"/>
    <EditText android:layout_width="match_parent"
              android:layout_height="wrap_content"
              android:inputType="textPassword"
              android:gravity="left"
              android:hint="@string/Pass_word"
              android:id="@+id/PassWordEditText"
              android:textColor="@color/yellow"
              android:textColorHint="@color/yellow"
              android:layout_above="@+id/ExitButtonInLoginPage"
              android:layout_alignParentStart="true" android:layout_marginBottom="59dp"/>
    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" app:srcCompat="@drawable/taxi2"
        android:layout_centerHorizontal="true"
        android:id="@+id/imageView" android:layout_above="@+id/UserNameEditText"/>
    <Button android:text="exit"
            android:layout_marginTop="20dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/yellow"
            android:id="@+id/ExitButtonInLoginPage"
            android:layout_marginBottom="24dp"
            android:layout_alignParentBottom="true" android:layout_alignParentStart="true"/>

</RelativeLayout>
